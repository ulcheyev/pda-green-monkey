class Item {
  constructor(amount, checked, measure, name, photo) {
    this.id;
    this.amount = amount;
    this.checked = checked;
    this.measure = measure;
    this.name = name;
    this.photo = photo;
  }
}
